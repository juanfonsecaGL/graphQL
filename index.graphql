schema {
    query: Query
    mutation: Mutation
}

type Mutation {
    createPerson(age: Int, name: String) : Person
}

type Query {
    person(name:String): Person
}

type Person {
    name: String
    age: Int
}